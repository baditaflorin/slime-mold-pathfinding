# Contributing

Thanks for helping improve `slime-mold-pathfinding`.

## Local Workflow

```sh
npm install
make install-hooks
make data
make test
make build
make smoke
```

Use Conventional Commits for commit messages, for example `feat: add route export`.

Do not commit secrets, `.env` files with real values, private keys, generated credentials, or private hostnames.
